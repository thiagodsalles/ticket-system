<%= form_for(@device) do |f| %>
  <% if @device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @device.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group field">
    <%= f.label :code %><br>
    <%= f.text_field :code, class: "form-control"   %>
  </div>
  
  

  <div class="form-group field">
    <%= f.label :local_id %><br>
    <%= collection_select(:device, :local_id, @local_options_for_select, :id, :room) %>
  </div>
  
  <div class="form-group field">
    <%= f.label :device_category_id %><br>
    <%= collection_select(:device, :device_category_id, @device_category_options_for_select, :id, :name) %>
    
  </div>
   
  <button type="submit" class="btn btn-primary">Submit</button>

<%end%>
